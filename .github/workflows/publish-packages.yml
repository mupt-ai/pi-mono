name: Publish Packages

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Publish packages
        run: |
          npm publish --workspace packages/ai --access public --provenance
          npm publish --workspace packages/tui --access public --provenance
          npm publish --workspace packages/agent --access public --provenance
          npm publish --workspace packages/coding-agent --access public --provenance
          npm publish --workspace packages/web-ui --access public --provenance
