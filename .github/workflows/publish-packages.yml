name: Publish Packages

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Show trusted publishing npm version
        run: npx -y npm@latest --version

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Publish @mupt-ai/pi-agent-core
        run: npx -y npm@latest publish --workspace packages/agent --access public --provenance

      - name: Publish @mupt-ai/pi-coding-agent
        run: npx -y npm@latest publish --workspace packages/coding-agent --access public --provenance
